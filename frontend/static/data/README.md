# Data fixtures

This folder contains small JSON fixtures used by the static frontend prototype. They mirror the outputs from `dss_analyst.py` (correlation, causality, regression, seasonality, forecasts) so the UI can be populated without a backend.
