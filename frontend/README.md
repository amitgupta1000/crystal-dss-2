# Frontend prototype

Open `frontend/static/index.html` in a browser to view the static prototype. The prototype loads JSON fixtures from `frontend/static/data/` and uses Chart.js for charts. Next steps: migrate this scaffold to React + Vite and wire to the backend.
